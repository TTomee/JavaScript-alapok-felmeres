let szam = Number(prompt("Add meg az osztani kívánt számot:"));
let oszto = Number(prompt("Add meg az osztót:"));

if (a % b == 0) {
    document.write(`${szam} / ${oszto} maradéka nem marad.`)
}

else {
    document.write(`${szam} / ${oszto} maradéka marad`);
}